%tr{"data-link": "#{new_transactions_path(customer)}", class: "cus-index__table__item"}
  %td.cus-index__table__item-id.center #{customer.id}
  %td.cus-index__table__item-image.center
    - if customer.image.present?
      = image_tag "#{customer.image.url}", class: "image"
    - else
      = image_tag "default.jpg", class: "image"
  %td.cus-index__table__item-name.left #{customer.name}
  %td.cus-index__table__item-kana.left #{customer.kana}
  %td.cus-index__table__item-sex.center #{sex_change(customer.sex)}
  %td.cus-index__table__item-birthday.center
    - if customer.birthday.present?
      #{customer.birthday}
  %td.cus-index__table__item-address.left #{full_address(customer)}
